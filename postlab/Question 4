function displayObject(obj) {
    for (let key in obj) {
        console.log(key + ": " + obj[key]);
    }
}

let person = {
    name: "Ruhitha",
    age: 19,
    city: "Hyderabad"
};

displayObject(person);